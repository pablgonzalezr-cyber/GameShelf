server:
  port: 8084

spring:
  application:
    name: ms-categoria

  datasource:
    url: jdbc:mysql://localhost:3307/db_categoria
    username: root
    password:

  jpa:
    hibernate:
      ddl-auto: update
    show-sql: true

eureka:
  client:
    service-url:
      defaultZone: http://localhost:8761/eureka/